"""SunGrid Support Agent — Streamlit demo entry point (Phase 4)."""

import streamlit as st

st.set_page_config(page_title="SunGrid Support Agent", layout="wide")
st.title("SunGrid Support Agent")
st.caption("Demo shell — UI arrives in Phase 4.")
